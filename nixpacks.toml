[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npm run build --workspace=client",
  "npm run build --workspace=server"
]

[start]
cmd = "npm run start"
